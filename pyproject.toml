[project]
name = "sora-sorter"
version = "0.1.0"
description = "Local SQLite-backed Sora video catalog"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
]

[tool.uv]
package = false
